## Обробка мови
```
python --version
py -3.12 -m venv .venv

.venv\Scripts\activate.bat
source .venv/bin/activate
python.exe -m pip install --upgrade pip
python3 -m pip install --upgrade pip

pip install gensim
pip install torch
pip install jupyter
pip install scikit-learn scipy numpy joblib nltk torch jupyter gensim

jupyter notebook