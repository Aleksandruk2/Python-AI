## Нейронні мережі
```
python --version
py -m venv .venv

.venv\Scripts\activate.bat
source .venv/bin/activate
python.exe -m pip install --upgrade pip
python3 -m pip install --upgrade pip

pip install jupyter

pip install numpy torch torchvision tqdm matplotlib scikit-learn

pip install numpy
pip install pandas
pip install torch
pip install torchvision
pip install tqdm
pip install matplotlib

pip install scikit-learn

jupyter notebook

```