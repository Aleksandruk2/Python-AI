## Використання gTTS - Phyton Python 3.14.5
```
python --version
py -m venv .venv

.venv\Scripts\activate.bat
python.exe -m pip install --upgrade pip
pip install jupyter

pip list

jupyter notebook
```